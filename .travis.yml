language: r
cache: packages

addons:
  apt:
    sources:
      - sourceline: 'ppa:opencpu/jq'

    packages:
      - libgdal-dev
      - libproj-dev
      - libgeos-dev
      - liblwgeom-2.1.2

sudo: required
dist: trusty

before_install:
 - sudo ldconfig
